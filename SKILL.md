---
name: en2zh-plain
description: 把英文的课件、讲义、教材章节、论文、技术文档翻译并改写成中文大白话学习笔记,输出为一个可分享的 HTML 网页。术语用「中文译名(English)」双语护航,难点配生活化类比,公式逐符号拆解,每节末尾附自测题。Use this skill whenever the user gives you English study material (a PDF lecture note, a .pptx deck, a course website link, a textbook chapter, a paper, or just pasted English text) and asks for it in Chinese, or asks to 翻译/讲讲/看不懂/说人话/大白话/通俗解释/整理成笔记 — even when they only say "帮我看看这个" with an English file attached, or ask for a Chinese summary of English material. Also trigger for "把这篇英文资料讲明白", "这章在说什么", "translate this lecture into Chinese", "帮我预习/复习这份英文材料". Do not use for translating Chinese into English, for casual chat translation of a single sentence, or for literary/marketing translation where faithfulness to style matters more than comprehension.
---

# 英文学习资料 → 中文大白话

## 这个 skill 要解决的真问题

把英文资料变成中文,难的从来不是翻译,是**换脑子**。

学术英语靠长句、名词化、被动语态、层层嵌套的从句来压缩信息。你如果逐词译过来,会得到一种技术上正确、读起来却比原文更累的中文——就是所谓「翻译腔」。学生读不懂英文原文,读完这种译文照样不懂,还多花了时间。

所以这个 skill 的工作不是 translate,是 **understand → re-express**:先自己把内容真正想明白,再用一个中国学生平时说话的方式重新讲一遍。中间那一步「真正想明白」不能跳,跳了就会退化成翻译腔。

因为同样的原因,**不要附一份逐句直译版**。有人会想「先给忠实译文再给白话版,两边都不耽误」,但那等于把翻译腔请回来,篇幅还翻倍。读者要的是一份能读懂的东西,不是一份对照表。

同时要守住一条底线:**大白话不等于说得含糊**。最常见的失败不是太难懂,而是听着顺了、信息却掉光了——把"在 X 条件下成立"简化成"成立",把充分条件说成必要条件,把具体数字换成"比较大"。读者拿着这份笔记去考试会挂科。通俗是指**表达方式**通俗,不是**内容**打折。

---

## 工作流程

### 1. 拿到材料

| 来源 | 怎么拿 |
|---|---|
| PDF 讲义、教材、论文 | 用 `pdf` skill 提取文字;扫描版先 OCR |
| .pptx 课件 | 用 `pptx` skill 提取,**务必连演讲者备注一起取** |
| 网页 / 课程站点 | `WebFetch` |
| 截图、拍的黑板照 | 直接 `Read` 图片 |
| 粘贴的文本 | 直接用 |

**演讲者备注为什么重要,以及怎么用。** 幻灯片上通常只有裸 bullet,真正的解释在老师嘴里,而备注是它离文字最近的残留。备注里有两类东西,性质完全不同,要分开处理:

- **知识点**——幻灯片上省略掉的解释。融进正文,读者不需要知道它来自备注。
- **元信息**——「这个每年必考」「学生每年都在这儿栽」。这是老师对考试的提示,不是知识本身。融进正文会被稀释掉,必须单独提成醒目标记,比如一个「老师在备注里强调」的方框。

这类元信息是整份材料里性价比最高的东西,漏掉很可惜。

材料超过约 30 页时,别闷头一次做完。先把「这章在讲啥」和第一个大节做出来给用户看,风格对了再继续。返工一整本的代价太大。

反过来,材料很短也不用心虚。一页讲义扩成八页是**正常的**——解释、类比、公式拆解、自测题都要占字数,原文省略的推导步骤还要补。篇幅膨胀是这件事的应有代价,不是你做过头了。

### 2. 先通读一遍,找主线

动笔之前先完整过一遍,回答一个问题:**这份材料到底想让读者获得什么能力?**

课件的章节顺序常常是按知识依赖排的,不是按理解难度排的。你通读之后往往会发现,第 4 节那个概念其实是全篇的核心,前三节都在为它做铺垫。知道这件事,你写第 1 节的时候就能提前打个招呼("这里先记着这个定义,第四节会用它做一件很关键的事"),读者就不会在前三节里迷路。

通读时还要留意**符号撞车**:同一份材料里用同一个字母表示两个无关的东西(比如噪声方差 σ² 和奇异值 σⱼ)、同一个词在不同章节含义漂移、上下标约定中途改变。这在真实讲义里非常常见,而且读者卡住的时候往往不知道自己卡在符号歧义上,会误以为是概念没懂。发现了就主动点破。

### 3. 建术语表

把所有专业术语拉一张表:中文译名、英文原词、一句话解释。

译名要用**该学科通行的中文叫法**,别自创。拿不准的时候搜一下,尤其是那些各家译法不统一的词。同一个词在全文必须用同一个译名,换来换去读者会以为是两个东西。

### 4. 逐节改写

这是主体工作,标准见下面「大白话怎么写」。

**怎么切节。** 按**主题**切,不按原文的页码或幻灯片编号切。9 张幻灯片切成 9 节太碎,合成 5 到 7 个主题才读得动。封面、目录、致谢、summary 页不单独成节。

**覆盖度。** 原材料里每一个实质性知识点,都要有对应的中文段落。可以合并相近的点,可以调整顺序让逻辑更顺,但不能因为某个点难讲就跳过——难讲的恰恰最需要讲。

**该动手算的时候动手算。** 教材经常只抛一个结论("互利贸易是可能的")却不给账,而省略掉的那步推导往往正是读者卡住的地方。有可验证的数值结论就自己算一遍,把中间步骤补上。注意边界:算术验证可以,改写材料的实质主张不可以。

**自己补的东西必须标出来。** 凡是原材料里没有、由你补出来的数字、推导、例子、背景知识,都要显式标注是你加的。读者复习时会默认页面上的东西都来自老师,把你算的数字当原文引用写进考卷,那就害了他。一个「补充(原材料没有)」的小标记就够。

### 5. 生成 HTML 网页

写页面之前先加载 `artifact-design`;如果要画机制图,再加载 `artifact-diagramming`。然后用 Artifact 工具发布。页面规格见下面「输出结构」。

---

## 大白话怎么写

### 反翻译腔:五个具体手法

**手法一:名词化的动作,改回动词。**

英语爱把动作包装成名词("the implementation of", "perform an analysis")。中文这么写就僵。

- ✗ 对输入数据进行归一化处理的目的是实现特征尺度的统一
- ✓ 先把输入数据缩到同一个量级。不然有的特征数值动辄上千,有的只有零点几,大的那个会把小的压得没声音

**手法二:被动语态,翻回主动。**

- ✗ 该参数被用于控制模型的复杂度
- ✓ 这个参数管的是模型能有多复杂

**手法三:长定语拆成短句。**

一句话里连续出现两个以上的「的」,基本就该重写了。

- ✗ 一种能够在不增加计算成本的前提下提升模型泛化能力的正则化方法
- ✓ 一种正则化方法,能让模型更会举一反三,而且不额外花算力

**手法四:抽象主语换成具体的人或物。**

- ✗ 研究表明,该方法的有效性得到了验证
- ✓ 有人拿真实数据试过,这招确实管用

**手法五:空心动词直接删掉。**

「进行分析」→「分析」或「算一算」。「作出决策」→「决定」。「实现优化」→「调快了」。「予以考虑」→「考虑」。

### 句子长度

一句话讲一件事。超过 40 个字就找个地方断开。

这不是风格洁癖。读者读不懂长句,是因为读到句尾时已经忘了句首的主语是什么。断句本质上是在帮他们把工作记忆腾出来。

### 语气

像一个耐心的学长在图书馆给你讲题:口语、直接、可以说"这块儿有点绕"、"这里容易掉坑"、"说白了就是"。

但别用力过猛。不要塞网络流行梗、不要加表情符号、不要每段都以"简单来说"开头——尤其不要用"简单来说"起头之后,跟一句同样难懂的话。

### 不能牺牲的东西

改写的时候,下面这些必须原样保留,一个都不能糊弄过去:

- **数字**:阈值、系数、复杂度、比例
- **条件和范围**:"当 n 足够大时"、"仅在独立同分布假设下"
- **例外**:"除了 X 以外都成立"里的那个 X
- **逻辑强度**:充分 ≠ 必要,相关 ≠ 因果,"可能"≠"一定","趋向于零"≠"等于零","缓解"≠"解决","单调不减"≠"递增"
- **方向**:增大还是减小、上界还是下界

逻辑强度这条最容易在通俗化过程中被冲掉,因为把 likely 说成"就会"、把 mitigate 说成"解决",句子会顺很多。但这些恰恰是考试爱考的点,也是论文里作者反复斟酌过的措辞。原文用了 hedge,译文就得留着 hedge。

如果原材料某处你自己也没读懂,或者你怀疑原文写错了,**如实说出来**,别糊一段看起来很通顺的话蒙过去。写成"原文这里说得含糊,我的理解是 XXX,建议你对照原文第 N 页确认一下"。学生宁可知道哪里有坑,也不想被一段流畅的错话带沟里。

---

## 术语:中英双语护航

术语首次出现时写成 **中文译名(English original)**,之后全文只用中文。

> 梯度下降(gradient descent)就是蒙着眼睛下山。你看不见路,但脚能感觉出哪边是下坡,就朝那边迈一小步。迈多大一步,这个事叫学习率(learning rate)。

为什么留英文:读者之后要去看英文论文、听老师用英文讲、上网搜资料、考试用英文答题。只给中文译名,等于让他在这些场合全部断线。

什么时候不留英文:日常词汇不用("data" 就写"数据"),已经完全汉化的常见词不用("算法"、"函数")。只给那些**真·专业术语**加英文,加多了页面会花。

**数学符号不适用这条规则。** λ、σⱼ、w 这类符号不要套「中文译名(English)」的格式。符号在公式的「念一遍」环节交代读音和含义,正文里直接用符号本身。

---

## 类比:必须标注失效边界

抽象概念配一个日常生活的例子,理解速度会快很多。但类比天生是不精确的——它靠"舍弃一部分真相"来换取"抓住主要矛盾"。

所以给了类比,就要说清楚它在哪儿不成立。否则读者会把类比当定义用,在考试里栽跟头。

> **打个比方**:哈希表(hash table)像一个有一万个格子的柜子,每份东西按名字算出一个格子号,直接扔进去。取的时候再算一次,直奔那个格子,不用一个个翻。
>
> **这个比方在哪儿不成立**:真柜子里两样东西不会占同一格,哈希表会——两个不同的名字算出同一个格子号,叫哈希冲突(hash collision),怎么处理冲突是这个数据结构最麻烦的部分,柜子的比喻完全没覆盖到。

**一节一个类比**,留给该节最抽象的那个概念。类比加上失效边界要占不少篇幅,四五个堆在一起会把正文冲散,读者反而记不住哪个对应哪个。硬找的类比比没有更糟。

---

## 公式:逐符号拆开讲

遇到公式,按三步走。原样搬过来再补一句"这个公式表示……"等于没讲。

**第一步,念一遍。** 每个符号读作什么、代表什么、取值范围或单位是什么。读者连符号都不认识的时候,后面说什么都是白说。

**第二步,讲形状。** 为什么是除法不是减法?分母变大会怎样?为什么要取对数?公式的结构本身就编码着作者的意图,这一步是把意图翻出来。

**第三步,代极端值检验。** 让读者把变量推到极限,看结果是否符合直觉。这是把公式从"一串符号"变成"能用的工具"的关键一跃。

三步是模板,不是硬性配额。有些公式是结构式(比如一个闭式解),能推的极端和上一个公式重复,那就把第三步并进「讲形状」,别硬凑。

举例:

> IDF(t) = log(N / n_t)
>
> **念一遍**:N 是文档总数,n_t 是含有词 t 的文档数。n_t 最小是 1,最大是 N。
>
> **讲形状**:分子固定、分母是"多少篇文档里出现过这个词"。词越常见,分母越大,整个分数越小。所以这个式子在给"稀有程度"打分。外面套 log 是为了压住数值——不然从出现在 1 篇到出现在 10 篇,分数会差 10 倍,差距太夸张。
>
> **推极端**:一个词每篇文档都有(n_t = N),分数是 log 1 = 0,权重归零。"的"、"是"这种词就是这么被自动过滤掉的,不需要额外维护停用词表。反过来,只在 1 篇里出现,分数拉到最大 log N。

---

## 图:什么时候值得画

原文**已有**的图表:说清楚横轴纵轴各是什么、单位是什么,然后指出这张图想让你看见的那个现象在哪儿。

原文**没有**图但值得补一张的情况——机制是循环、状态机、多步因果链、或者有并列结构需要对比时,纯文字讲损失很大。这时候画一张内联 SVG,并加载 `artifact-diagramming` 拿画法。

判断标准很简单:如果你发现自己在用"先……然后……接着……最后回到"这种句式描述一个绕回起点的过程,那就该画图了。

---

## 自测题

每节末尾放 2 到 3 道题,答案折叠起来。这里的「节」指正文的主题节,术语表和开头的概述不算。

出题的原则是**考理解,不考记忆**。"X 的定义是什么"这种题没价值,翻回去就能抄到答案,答对了也不代表懂了。要问的是:

- 如果把 X 改成 Y,会发生什么?
- 下面哪种情况下这个方法**不**适用?
- A 和 B 看起来很像,区别在哪?
- 为什么这里用的是 X 而不是更简单的 Y?

答案不要只写结论,补一句为什么。读者答错的时候,需要知道自己是哪一步想歪了。

---

## 输出结构

一个 HTML 页面,按这个顺序。下面这些名字是**内容说明,不是规定的字面标题**——用贴合这份材料的中文标题就好,但四个区块都要有,顺序不要变。

**1. 主线导读** — 3 到 5 句话说清主线。不是罗列小标题,是回答"读完这份材料你能做什么以前做不了的事"。读者带着这个框架往下读,细节才挂得住。

**2. 术语速查表** — 三列:中文 / English / 一句话解释。放在前面,方便读到一半忘了回头扫一眼。

**3. 正文** — 按主题切节,每节包含:
- 标题:中文为主,英文原标题小字附在旁边
- 大白话正文
- 类比(至多一个)
- 公式解读(有公式时)
- 机制图(值得画时)
- 自测题,答案用 `<details>` 折叠

**4. 收尾回顾** — 全篇 3 到 5 个要点,回应开头的主线导读,让读者确认自己拿到了。

### 页面技术规格

**公式渲染。** 简单公式(单行、没有分式叠加或大型求和号)直接用 HTML 加 CSS 排,不值得为它拉一个库。真正复杂的数学才上 MathJax,从 cdnjs 加载 SVG 版:

```html
<script src="https://cdnjs.cloudflare.com/ajax/libs/mathjax/3.2.2/es5/tex-svg.js"></script>
```

**不要用 KaTeX 的 CDN 链接。** Artifact 的内容安全策略只放行 cdnjs 上的**脚本**,它的样式表和字体文件会被静默拦截。KaTeX 依赖自己的 CSS 和一组 woff2 字体,少了它们会渲染成一堆错位的 span,比不渲染还难看,而且没有任何报错提示。MathJax 的 tex-svg 版本把公式渲染成内联 SVG,不请求外部 CSS 也不请求字体,所以是安全的。

不管用哪种方式,都别让页面在任何失败路径下露出裸的 LaTeX 源码。

**中文字体。** 中文字体文件动辄几 MB,不要试图从 Google Fonts 加载,等于让页面卡死。用系统字体栈:

```css
font-family: "PingFang SC", "Microsoft YaHei", "Hiragino Sans GB",
             "Source Han Sans SC", "Noto Sans SC", sans-serif;
```

这意味着 `artifact-design` 里「挑一对字体」的指导,在中文页面上实际只对拉丁字母和数字生效。西文和数字可以单独指定一个 Google Fonts 字体(等宽字体尤其值得,数字对齐会好看很多),中文部分老老实实用系统字体。

**折叠规则。** 术语表用 `<details open>`——可折叠,但默认展开。自测题的**答案**默认折叠,题目本身要可见。正文一律不折叠。学习材料默认藏内容会让人错过东西,这是底线;折叠只用来收纳「暂时不看也行」的部分。

**页面标题。** 用原材料的章节名译名,2 到 4 个词,不要加解释性后缀。比如「正则化与偏差方差权衡」。

**其他。** 窄屏不横向滚动;深浅色主题都正常;表格和图各自放在能横向滚动的容器里。视觉设计交给 `artifact-design`。

---

## 一个完整的小例子

原文:

> Backpropagation computes the gradient of the loss function with respect to each weight by the chain rule, computing the gradient one layer at a time, iterating backward from the last layer to avoid redundant calculations of intermediate terms in the chain rule.

翻译腔版本(不要这样):

> 反向传播通过链式法则计算损失函数关于每个权重的梯度,一次计算一层的梯度,从最后一层开始向后迭代,以避免链式法则中中间项的冗余计算。

大白话版本:

> 神经网络训练时要知道"每个权重该往哪个方向调、调多少",这个信息就是梯度(gradient)。
>
> 问题是网络有很多层,一个靠前的权重要影响最终结果,得一层层传过去。用链式法则(chain rule)硬算的话,每个权重都要把整条链重算一遍,而不同权重的链条有大量重复部分,算了无数次重复劳动。
>
> 反向传播(backpropagation)的办法是**倒着来**:从最后一层往前推,每往前一层,就把上一层已经算好的结果拿来接着用。重复的部分只算一次。
>
> 说白了就是动态规划那一套思路,只不过用在了求导上。

注意这个版本做了什么:它把原文一句话拆成四段,补上了原文没写但读者必需的背景(梯度是干嘛的、为什么会有重复计算),用"倒着来"、"接着用"这种口语动词替换了"迭代"、"冗余计算",最后给了一个能挂靠的已知概念(动态规划)。信息一点没少,还多了。
